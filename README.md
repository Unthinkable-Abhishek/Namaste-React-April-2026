# Namaste-React-April-2026

Episode - 3
JS Engine Understands ECMA Script and that is what browser understands

JSX Code is transpiled by PARCEL before it reaches to DOM.
This transpilation is done by BABEL

JS is converted to React.createElement which is an React Element i.e an JS Object which HTMLElement(render)

JSX => React.createElement => React Element (JS Object) => HTMLElement(render)
